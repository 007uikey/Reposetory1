pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {

        stage('Clean Old Deployment') {
            steps {
                sh 'rm -rf /var/www/html/*'
            }
        }

        stage('Deploy New Files') {
            steps {
                sh 'cp -r * /var/www/html/'
                sh 'chown -R apache:apache /var/www/html'
            }
        }

    }

    post {
        success {
            echo 'Repo1 deployed successfully'
        }
    }
}
