pipeline {

    agent { label 'slave2' }

    stages {

        stage('Deploy') {

            steps {

                sh '''
                sudo rm -rf /var/www/html/*
                sudo cp index.html /var/www/html/
                '''

            }
        }
    }
}
